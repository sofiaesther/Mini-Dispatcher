# PostgreSQL connection string — use YOUR actual username and password.
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
# If you get "Authentication failed" (P1000), the USER or PASSWORD above are wrong for your PostgreSQL.
DATABASE_URL="postgresql://postgres:YOUR_PASSWORD@localhost:5432/mini_dispatcher"

PORT=3001
